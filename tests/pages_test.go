package tests

import (
	"testing"
)

func TestHomePage(t *testing.T)  {
	//baseURL := "http://localhost:3000"
	//
	//var (
	//	resp *http.Response
	//	err  error
	//)
	//// 1. 请求 模拟用户访问浏览器
	//resp, err = http.Get(baseURL + "/")
	//
	//// 2. 检测 是否无错误且是200
	//assert.NoError(t, err, "有错误发生，err不为空")
	//assert.Equal(t, 200, resp.StatusCode, "应用返回状态吗 200")
}